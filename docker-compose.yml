services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: personfinder
      POSTGRES_USER: personfinder
      POSTGRES_PASSWORD: personfinder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
